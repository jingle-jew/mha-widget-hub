{
  "title": "MHA Widget Hub",
  "config": {
    "step": {
      "user": {
        "title": "Set up MHA Widget Hub",
        "description": "Choose optional MHA sidebar modules. The main MHA dashboard is always enabled.",
        "data": {
          "admin": "MHA Admin",
          "diagnostics": "MHA Insights"
        }
      }
    },
    "abort": {
      "already_configured": "MHA Widget Hub is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "MHA Sidebar Modules",
        "description": "Choose which optional MHA modules should appear in the Home Assistant sidebar. The main MHA dashboard remains enabled.",
        "data": {
          "admin": "MHA Admin",
          "diagnostics": "MHA Insights"
        }
      }
    }
  }
}
