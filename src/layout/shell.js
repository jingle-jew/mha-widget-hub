import {createDock} from "./dock.js";import {createStatusBar} from "./status-bar.js";
export function createShell(meta={}){const bg=document.createElement("div");bg.className="mha-background";bg.setAttribute("aria-hidden","true");for(let i=0;i<4;i++)bg.append(document.createElement("span"));const shell=document.createElement("main");shell.className="mha-shell";const grid=document.createElement("section");grid.className="mha-grid";grid.setAttribute("aria-label","Grille de widgets vides");shell.append(createStatusBar(meta),grid,createDock());return{bg,shell,grid}}
